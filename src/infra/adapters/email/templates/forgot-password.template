Hello <%= user.name %>,

We received a request to reset your password. Click the <a href="<%= resetLink %>">link</a> to reset your password

In case the link didn't work here is it:
<%= resetLink %>

If you did not request this, please ignore this email.

Best,
The Team